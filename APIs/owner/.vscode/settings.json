{
    "sqltools.connections": [
        {
            "previewLimit": 50,
            "driver": "SQLite",
            "name": "owner",
            "database": "${workspaceFolder:owner}/owners.db"
        }
    ]
}